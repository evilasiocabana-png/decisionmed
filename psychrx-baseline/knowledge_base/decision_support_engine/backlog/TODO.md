# Decision Support Engine Backlog

## Next Build Steps

1. Load `medication_strategy_table.csv` into the local Python table instead of duplicating values in code.
2. Load `decision_rules.csv` into the rule engine.
3. Make the UI generate the next missing question from `question_derivation_matrix.csv`.
4. Add confidence scoring based on field completeness and safety closure.
5. Expand medications:
   - fluoxetine
   - paroxetine
   - fluvoxamine
   - vortioxetine
   - agomelatine
   - trazodone
   - TCAs
   - mood stabilizers
   - antipsychotic augmentation candidates
6. Add source-level review status per row:
   - draft
   - reviewed
   - published_internal
   - runtime_eligible_future

## Open Design Questions

- Should the table separate drug candidate from drug class candidate?
- Should augmentation candidates require a separate safety matrix?
- How should contraindications suppress specific candidates?
- Should the next UI question be generated by priority or by current rule path?
