# Registry Compatibility Namespace

This folder preserves the Program 10 Phase 1 registry naming contract.

The current runtime uses explicit structural composition in `clinical_runtime/runtime.py`.

No clinical engine registry or runtime plugin registry is implemented here.
