# 188 - Application Kernel Adapter

`ApplicationKernelAdapter` criado para permitir que a Application Layer consuma `KernelResult` estrutural.

Declaracao Final: adapter nao cria endpoint clinico novo e nao gera decisao clinica.
