# DM-071 — Readiness gate visibility

The DecisionMEd hub now presents the platform readiness gates returned by the existing read-only application state. Each item shows its machine-readable key, current status and recorded reason.

This is operational transparency, not a clinical feature. The page still has no patient-value input path, and a gate's presence never changes the blocked clinical-execution state.
